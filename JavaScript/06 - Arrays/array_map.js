// Método .filter()

const numeros = [1, 2, 3, 4, 5]

const resultado  = numeros.map( elemento => elemento * 3 );

console.log(resultado);


const nombres = ["Arturo", "Solange", "Gaston", "Lucas", "Nicolas"];

const result = nombres.map(x => x.length);
console.log(result);
